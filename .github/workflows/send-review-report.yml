name: Send Code Review Report

on:
  workflow_dispatch:

jobs:
  send-telegram:
    runs-on: ubuntu-latest
    steps:
      - name: Send Telegram message
        run: |
          MESSAGE=$(cat << 'MSGEOF'
🔍 コードレビュー報告（2026-06-05）

【致命的】

📁 api/notify-telegram.js:28-36
⚠️ CRON_SECRET未設定時は認証を完全スキップ。?test=1クエリで誤れるTelegram送信が認証なしで可能。
💡 CRON_SECRET未設定でもtest=1以外は401を返す、またはCRON_SECRET必須化。

📁 api/post-x.js:38-44 / api/daily-report.js:33-39
⚠️ 同様にCRON_SECRET未設定時は認証スキップ。本番で未設定のまま運用すると任意リクエストでX投稿・レポート送信が実行される。
💡 CRON_SECRET未設定時は500を返すかバリデーション必須化。

📁 vite.config.js（handleTrackProxy）
⚠️ ローカル開発のCORSが Access-Control-Allow-Origin: * で本番制限と真逆。任意オリジンからtracking APIへ書き込み可能。
💡 本番同様に localhost:5173 のみ許可する。

【改善推奨】

📁 api/deck-cache.js:632-684（consumeCredit）
⚠️ readCredits→upsertの2ステップに競合状態。並列リクエストでクレジットを二重消費する可能性。
💡 Supabase RPCでアトミックインクリメントに変更。

📁 api/daily-report.js:140-148
⚠️ eventsをLIMIT(5000)全取得後JSフィルタ。ユーザー増加時に5000件超でデータ欠落。
💡 .gte("occurred_at", todayStart)をSupabaseクエリに追加。

📁 src/api.js:156-163（aiEval）
⚠️ term・correctDef・userAnsをサニタイズなしでLLMプロンプトに埋め込み。悪意あるカードでプロンプトインジェクションが可能。
💡 埋め込み前に200文字にsliceするか構造化出力を使う。

📁 api/public-library.js:116（handlePublish）
⚠️ source_deck_idがnullの場合、upsertのonConflict照合が機能せずINSERTが重複実行される可能性。
💡 source_deck_idがnullの場合は別ロジックで処理。

【軽微】

📁 vite.config.js（parseDeckPayload）
⚠️ api/deck-cache.jsと同等の関数が重複実装。こちらは古いバージョン（<think>除去なし）。

📁 src/App.jsx:327
⚠️ window.confirm()による削除確認。モバイルブラウザでブロックされる場合がある。

📁 api/daily-report.js:225
⚠️ Google SheetsのスプレッドシートIDがハードコード。URL変更時に気づきにくい。

---
📊 分析ファイル数: 30件
MSGEOF
          )
          
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -H "Content-Type: application/json" \
            -d "{\"chat_id\": \"${{ secrets.TELEGRAM_CHAT_ID }}\", \"text\": $(echo "$MESSAGE" | python3 -c 'import json,sys; print(json.dumps(sys.stdin.read()))')}"
