# MNEMOX プロジェクト設定

## 概要

AI生成単語帳アプリ。AIを使って簡単にデジタル単語帳を作成できる。作成した単語帳で学習をすることができる。主に言語とユーザーが学びたい専門分野（例：IT用語、心理学など自由）の学習に使用されることを想定。将来的にiOSアプリ版とweb版を用意する。

## 機能

### 基本機能
- 単語の言語：全言語（無理のない範囲）＋専門用語
- 定義の言語：全言語
- 定義の詳細さを３段階で選択可能（１：簡易、２：普通、３：詳細）
- 作成した単語帳はその後も自由に編集できる

### 作成機能
1. **手動で新規作成**：単語帳を新規作成し、ユーザーが単語カードに単語・用語（例：go、CPU）を入力すると、定義カードにAIが自動で定義を出力する。AIによる自動生成はオフにすることもできる。
2. **AIで自動作成**：チャットで「量子力学の入門を学びたい！」など分野・テーマを入力すると、AIが自動で単語帳を作成する。

### 学習機能
1. **フラッシュカード暗記**
2. **選択クイズ学習**
3. **テスト**：全問正解するとその単語帳を「クリア」したと判定する

## 技術スタック
- フロントエンド: Vite + React
- バックエンド: Vercel Serverless API
- DB: Supabase（オプション）
- AI: Ollama (qwen3:1.7b) → Groq → Gemini の順でフォールバック

## 作業方針
- UIの基本フォーマットは日本語。単語・定義の言語は全言語対応
- ユーザーはPC・IT・AI初心者のため、専門用語を使わない
- 実行できる作業はすべて自分で行い、ユーザーの手を煩わせない
- ユーザーが操作する必要がある場合は、極限までわかりやすく・簡単な言葉で・番号付きの手順で説明する
- mnemoxに直接的な変更を加えてユーザーが動作確認をする必要がある場合は、発言の最後に http://localhost:5173 のリンクを貼る
- アプリの表示・UI・コード上のあらゆる場所に「Quizlet」という名前を一切使用しない
- AI APIの呼び出しは必ずバックエンド（/api/）から行い、フロントエンドから直接呼ばない
